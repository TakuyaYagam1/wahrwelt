name: Linting

on:
  pull_request:
  push:
    branches:
      - main
      - dev
    paths:
      - "Linux/**"
      - "README.md"
      - ".gitignore"
      - ".github/dependabot.yml"
      - ".github/workflows/**"

jobs:
  linting:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.1

      - name: Set up Go
        uses: actions/setup-go@v7.0.0
        with:
          go-version-file: Linux/installer/go.mod
          cache-dependency-path: Linux/installer/go.sum

      - name: Install Nix
        uses: cachix/install-nix-action@v31.11.1
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes

      - name: Install shell-check toolchain with Nix
        id: tools
        run: |
          profile="$RUNNER_TEMP/wahrwelt-linting-tools"
          nix profile add --profile "$profile" --inputs-from "$GITHUB_WORKSPACE" \
            nixpkgs#actionlint \
            nixpkgs#coreutils \
            nixpkgs#fish \
            nixpkgs#nix \
            nixpkgs#util-linux \
            nixpkgs#shellcheck \
            nixpkgs#jq

          profile_store="$(readlink -e -- "$profile")"
          case "$profile_store" in
            /nix/store/*) ;;
            *) echo "::error::Tool profile did not resolve into the Nix store"; exit 1 ;;
          esac
          for tool in actionlint fish jq mv nix nix-store shellcheck unshare; do
            test -x "$profile_store/bin/$tool" || {
              echo "::error::Missing CI tool: $tool"
              exit 1
            }
          done

          echo "profile_bin=$profile_store/bin" >> "$GITHUB_OUTPUT"

      - name: Prepare installer test host
        env:
          CI_PROFILE_BIN: ${{ steps.tools.outputs.profile_bin }}
        run: |
          case "$CI_PROFILE_BIN" in
            /nix/store/*/bin) ;;
            *) echo "::error::Tool profile output is not an immutable store bin"; exit 1 ;;
          esac
          for tool in actionlint fish jq mv nix nix-store shellcheck unshare; do
            test -x "$CI_PROFILE_BIN/$tool" || {
              echo "::error::Missing pinned CI tool: $tool"
              exit 1
            }
          done
          for tool in nix nix-store; do
            test "$(stat -c '%u' -- "$CI_PROFILE_BIN/$tool")" = 0 || {
              echo "::error::Pinned $tool invocation is not root-owned"
              exit 1
            }
            test "$(stat -Lc '%u' -- "$CI_PROFILE_BIN/$tool")" = 0 || {
              echo "::error::Pinned $tool executable is not root-owned"
              exit 1
            }
          done
          export PATH="$CI_PROFILE_BIN:$PATH"
          mv --help | grep -Fq -- '--exchange'
          mv --help | grep -Fq -- 'none-fail'
          if ! unshare -Ur true 2>/dev/null; then
            if [ -e /proc/sys/kernel/apparmor_restrict_unprivileged_userns ]; then
              sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
            fi
          fi
          unshare -Ur true
          nix --version
          nix-store --version

      - name: Lint GitHub Actions workflows
        env:
          CI_PROFILE_BIN: ${{ steps.tools.outputs.profile_bin }}
        run: PATH="$CI_PROFILE_BIN:$PATH" actionlint .github/workflows/*.yml

      - name: Run shell, Lua, and selector integration gates
        env:
          CI_PROFILE_BIN: ${{ steps.tools.outputs.profile_bin }}
        run: PATH="$CI_PROFILE_BIN:$PATH" make -C Linux test-hypr-integration

      - name: Realize managed QuickShell transition artifact
        env:
          CI_PROFILE_BIN: ${{ steps.tools.outputs.profile_bin }}
        run: PATH="$CI_PROFILE_BIN:$PATH" make -C Linux nix-shell-transition-build

      - name: Run linting
        env:
          CI_PROFILE_BIN: ${{ steps.tools.outputs.profile_bin }}
        run: PATH="$CI_PROFILE_BIN:$PATH" make -C Linux lint

      - name: Run Go tests
        env:
          CI_PROFILE_BIN: ${{ steps.tools.outputs.profile_bin }}
        run: PATH="$CI_PROFILE_BIN:$PATH" make -C Linux test-go

      - name: Verify cumulative package presets
        env:
          CI_PROFILE_BIN: ${{ steps.tools.outputs.profile_bin }}
        run: PATH="$CI_PROFILE_BIN:$PATH" make -C Linux nix-package-preset-contract

      - name: Verify least-privilege container runtime contract
        env:
          CI_PROFILE_BIN: ${{ steps.tools.outputs.profile_bin }}
        run: PATH="$CI_PROFILE_BIN:$PATH" make -C Linux/installer nix-hardening-contract

      - name: Evaluate mixed-version Noctalia Home Manager module
        env:
          CI_PROFILE_BIN: ${{ steps.tools.outputs.profile_bin }}
        run: PATH="$CI_PROFILE_BIN:$PATH" make -C Linux/installer nix-noctalia-hm-compat

      - name: Realize validated End4 Hypr artifact
        env:
          CI_PROFILE_BIN: ${{ steps.tools.outputs.profile_bin }}
        run: PATH="$CI_PROFILE_BIN:$PATH" make -C Linux nix-end4-hypr-build

      - name: Realize managed End4 pC QuickShell artifact
        env:
          CI_PROFILE_BIN: ${{ steps.tools.outputs.profile_bin }}
        run: PATH="$CI_PROFILE_BIN:$PATH" make -C Linux nix-end4-pc-quickshell-build

      - name: Build v1 to v2 migration unit
        env:
          CI_PROFILE_BIN: ${{ steps.tools.outputs.profile_bin }}
        run: |
          export PATH="$CI_PROFILE_BIN:$PATH"
          nix build --no-link --no-write-lock-file \
            "path:${GITHUB_WORKSPACE}?dir=Linux/NixOS#nixosConfigurations.NixOS.config.systemd.units.\"wahrwelt-v1-to-v2-migration.service\".unit"

      - name: Evaluate preset NixOS hosts
        env:
          CI_PROFILE_BIN: ${{ steps.tools.outputs.profile_bin }}
        run: |
          export PATH="$CI_PROFILE_BIN:$PATH"
          for preset in minimal desktop developer personal; do
            nix eval --raw --no-write-lock-file \
              "path:${GITHUB_WORKSPACE}?dir=Linux/NixOS/presets/${preset}#checks.x86_64-linux.preset-host.drvPath"
          done
