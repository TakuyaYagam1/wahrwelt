--- a/ii/modules/ii/background/Background.qml
+++ b/ii/modules/ii/background/Background.qml
@@ -1,4 +1,5 @@
 pragma ComponentBehavior: Bound
+// End4 live wallpaper contract anchor.

 import qs
 import qs.services

--- a/ii/scripts/colors/switchwall.sh
+++ b/ii/scripts/colors/switchwall.sh
@@ -107,4 +107,5 @@ RESTORE_SCRIPT="$RESTORE_SCRIPT_DIR/__restore_video_wallpaper.sh"
 THUMBNAIL_DIR="$RESTORE_SCRIPT_DIR/mpvpaper_thumbnails"
 VIDEO_OPTS="no-audio loop hwdec=auto scale=bilinear interpolation=no video-sync=display-resample panscan=1.0 video-scale-x=1.0 video-scale-y=1.0 video-align-x=0.5 video-align-y=0.5 load-scripts=no"
+RESTORE_SHELL="${BASH:-bash}"

 is_video() {
@@ -117,16 +118,18 @@ kill_existing_mpvpaper() {
 }

 create_restore_script() {
-    local video_path=$1
+    local video_path="$1"
+    local escaped_video_path
+    printf -v escaped_video_path '%q' "$video_path"
     cat > "$RESTORE_SCRIPT.tmp" << EOF
-#!/bin/bash
+#!$RESTORE_SHELL
 # Generated by switchwall.sh - Don't modify it by yourself.
 # Time: $(date)

 pkill -f -9 mpvpaper

 for monitor in \$(hyprctl monitors -j | jq -r '.[] | .name'); do
-    mpvpaper -o "$VIDEO_OPTS" "\$monitor" "$video_path" &
+    mpvpaper -o "$VIDEO_OPTS" "\$monitor" $escaped_video_path &
     sleep 0.1
 done
 EOF
@@ -135,7 +138,7 @@ create_restore_script() {

 remove_restore() {
     cat > "$RESTORE_SCRIPT.tmp" << EOF
-#!/bin/bash
+#!$RESTORE_SHELL
 # The content of this script will be generated by switchwall.sh - Don't modify it by yourself.
 EOF
     mv "$RESTORE_SCRIPT.tmp" "$RESTORE_SCRIPT"
